{
  "name": "biartworks",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api:dev": "pnpm --filter api start:dev",
    "build": "pnpm --filter api build; pnpm --filter admin build",
    "schema": "pnpm --filter api schema",
    "web:dev": "pnpm --filter web dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
